// The domain layer: framework-free entity types that mirror the database.
// No React, no Next — safe to share with any future client (web Headquarters)
// or extract into `packages/domain`. Import from `@/lib/domain`.
export * from "./types";
